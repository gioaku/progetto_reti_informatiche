***DS***
PeerList
peer msg
    boot
        insert peer
        send ack and nbs
        update nbs for others
        ++ send date
    exit
        remove peer 
        send ack
        update nbs
stdin msg
    help
        print help
    showpeers
        print peer ports
    showneighbor arg
        if arg print nb(arg)
        else print nb(all)
    esc
        send exit udp to all peers
        wait for acks
        exit

++ timer 18
    send_udp_wait_ack DAY_RSET